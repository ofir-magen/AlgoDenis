# Single env for the whole project

# JWT secret
SECRET_KEY=change-me-to-a-long-random-secret

# Frontend origins for CORS (comma-separated)
FRONTEND_ORIGINS=http://localhost:5180,http://127.0.0.1:5180

# Existing users DB (SQLite) from your other sites
USERS_DB_PATH=../backend/Users.db

# Schema mapping (adapt to your DB)
USERS_TABLE=users
USERNAME_COL=username
PASSWORD_HASH_COL=password_hash
ACTIVE_COL=is_active

# Password hash scheme used in your DB: bcrypt | plain
HASH_SCHEME=bcrypt

# Backend port
API_PORT=8020
